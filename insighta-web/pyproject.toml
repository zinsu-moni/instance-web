[project]
name = "insighta-web"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn",
  "jinja2",
  "python-dotenv",
  "httpx",
  "python-multipart"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.uvicorn]
factory = false
